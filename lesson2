def square_digits(num):
    num = list(str(num))
    num = [ int(elem) for elem in num]
    num = [ * (x*x for x in num)]
    num = (''.join(map(str, num)))
    num = int(num)
    return num
    pass
    
    def square_digits(num):
    return int(''.join(str(int(d)**2) for d in str(num)))
